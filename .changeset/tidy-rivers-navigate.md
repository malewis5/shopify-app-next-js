---
"shopify-app-react": patch
---

Add React utilities for embedded Shopify apps: `ShopifyHead` configures App Bridge and Polaris web components, while `AppProvider` and `useShopifyNavigation` delegate same-origin Shopify navigation events to an injected client-side navigation function.
